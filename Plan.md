# 한국어 PRM 라벨 투영 계획 v9.2

작성일 2026-09-20. 산출물은 영어 PRM의 판정을 번역 다리로 빌려 한국어 풀이에 하드 라벨을 붙이고, 그 라벨로 학습한 한국어 PRM이다. 평가는 기존 KO MATH500 하네스로 한다. 목적은 SOTA가 아니라 이 방법의 가능성을 적은 데이터로 보이는 것이다.

v9.2에서 바뀐 것은 13.1절에, v9.1의 변경은 13.2절에 있다. 학생 구조를 Qwen2.5-Math-PRM의 실제 코드에 맞춰 고정하고 폴백을 정했으며, Math-Shepherd를 기준으로 라벨 정의와 증거의 위치를 정리했고, 최신 Qwen 계열로의 교체는 12.4절의 가능성으로 옮겼다. 생성기와 학생 백본은 기존 측정과의 연속성을 위해 EXAONE-4.0-1.2B와 Qwen2.5 계열을 유지한다.

## 1. 주장과 증거의 형태

주장은 하나다. 단일언어 PRM을 새 언어로 넓히는 데 필요한 것은 번역기와 원래 PRM뿐이다. 교사(Qwen2.5-Math-PRM-72B)의 판정은 번역 다리를 건너 빌려 오고, 언어와 무관하게 공짜로 얻는 정오 y가 그 판정을 굳힌다. 사람 라벨도, 대상 언어의 판정 모델도 새로 만들지 않는다.

비교 대상은 셋이다.

| 팔 | 학습 텍스트 | 라벨 출처 |
|---|---|---|
| A | PRM800K 풀이를 영→한 번역 | 사람 라벨 그대로 |
| B | 한국어 생성기의 온폴리시 원문 | 한→영 번역, 교사 채점, y 조건부 커널 |
| A+B | 둘을 반씩 | 위와 같음. 라벨 형식이 같아 손실 변경 없이 섞인다 |

A는 기존 다국어 PRM 연구의 방식이다. A는 위치가 정밀한 사람 라벨과 그럴듯한 오답을 주지만 텍스트가 번역문이고, B는 실제 입력 분포와 정답 풀이를 주지만 첫 오류 위치에 교사와 번역의 잡음이 있다.

증거는 절대 성능이 아니라 세 가지 상대 비교로 둔다.

1. 같은 크기에서 B가 A 이상이고, A+B가 둘 이상이다.
2. 3천, 6천, 1.2만 풀이의 학습 곡선이 1.2만 지점에서 아직 올라가고 있다.
3. 현행(영어 PRM을 한국어 원문에 직접)과 번역 경유 교사는 참조선으로 같이 그리되 통과 기준으로 쓰지 않는다.

이 형태의 선례는 Math-Shepherd에 있다.

- 1만 규모의 상대 비교. 학습 데이터 1만 개에서도 PRM이 ORM보다 약 4%p 높았고, PRM 쪽의 데이터 효율이 더 좋다고 보고했다. 다만 그 검증기는 7B 이상이어서 1.2B에서 같은 폭이 나온다는 보장은 없다.
- A 대 B. MATH에서 자동 라벨로 학습한 PRM이 PRM800K로 학습한 PRM을 앞섰다. 저자들은 원인으로 PRM800K가 GPT-4 출력에 붙은 라벨이라 오픈 모델 출력과 분포가 다르다는 점과, 자동 라벨 쪽 데이터가 4배 많다는 점을 함께 들었다. 우리는 A와 B의 크기를 맞추므로 분포 효과만 분리해서 본다. 같은 논문의 한계 절은 사람 라벨과 자동 라벨의 통합을 과제로 남겼는데, 그것이 A+B다.

1.2B를 처음부터 1.2만 풀이로 학습해 현행을 넘기는 것은 기대하지 않는다. 공개 PRM들은 수십만 풀이 규모로 학습됐다.

## 2. 라벨의 의미와 커널

### 2.1 의미

스텝 라벨은 "이 접두가 아직 정답에 닿을 수 있는가"다. 1은 닿을 수 있음, 0은 이미 틀림, 마스크는 판단 보류다. 누적 의미이므로 한 풀이의 라벨은 항상 [1…1, ·…·, 0…0] 꼴이다.

이 정의는 Math-Shepherd의 하드 추정(HE)과 같다. HE는 스텝에서 이어 쓴 완성 중 하나라도 정답에 닿으면 1, 아니면 0이고, Math-Shepherd는 이 이진 라벨로 학습했다. 성공 비율을 그대로 쓰는 소프트 추정(SE)과 검증기 성능에 실질적인 차이가 없었다고도 보고했다. 우리 커널은 HE를 롤아웃 없이 추정한다.

- y=1이면 실제로 이어진 풀이가 정답에 닿았으므로 모든 접두가 증명된 1이다(HE=1). 교사가 낮은 점수를 준 스텝이 있어도 이 증명이 우선한다.
- y=0이면 마지막 스텝은 확실한 0이다. 그 앞은 교사 점수로 0이 되는 구간을 추정한다.

Math-Shepherd와 다른 점은 0의 출처다. Math-Shepherd는 완성기가 실패하면 0을 찍기 때문에 완성기가 약하면 멀쩡한 접두도 0이 되고, 저자들도 완성기의 능력이 주석 품질을 좌우한다고 보고했다. 우리는 0을 교사의 판정에서 가져온다.

### 2.2 교사 점수 추출

교사 입력은 `"<extra_0>".join(steps_en) + "<extra_0>"`로 직접 조립한다. 시스템 프롬프트는 모델 카드의 것을 쓰고 query는 원문 영어 문제다. 교사는 인과 마스크 모델이라 t번째 점수는 t번째 스텝까지의 접두만 본다.

점수는 확률이 아니라 로그 오즈로 캐시한다. `<extra_0>` 위치의 2-class 로짓을 float32로 올려 z_t = l1 − l0을 저장한다. bf16 확률은 1 근처에서 127/128, 1.0 같은 값으로 뭉쳐서 천장과 하강폭 계산이 깨지고, 모델 카드의 `sample[sample != 0]` 필터는 확률이 정확히 0으로 떨어지면 짝이 어긋난다.

### 2.3 기준 분포

절대 임계는 쓰지 않는다. 번역문의 어색함이나 생성기 문체 때문에 교사가 멀쩡한 스텝에도 일괄적으로 낮은 점수를 주는 풀이가 있기 때문이다. 기준은 y=1 풀이에서 만든다. y=1 풀이의 스텝은 전부 1이므로, 거기서 나온 분포가 "멀쩡한 스텝이 이 번역과 이 생성기를 거치면 얼마나 출렁이는가"의 실측이다. 생성기별로 따로 만든다.

- 풀이의 천장 b: 그 풀이 안 z의 상위 25% 지점
- b_min: y=1 풀이들의 천장 분포에서 하위 5% 지점
- 하강폭 d_t = max(b, b_min) − z_t
- D_g: 생성기 g의 y=1 풀이 전 스텝에서 모은 d의 분포

b_min은 처음부터 틀려서 전 스텝이 낮은 풀이를 거르는 장치다. 자기 천장만 쓰면 그런 풀이가 멀쩡해 보인다.

### 2.4 커널 (y=0 풀이)

```
π_t  = P_{D_g}(d ≥ d_t)                 정답 풀이 스텝 중 이만큼 이상 떨어지는 비율
후보 C = {t : π_t < 0.05}
p'_t = π_t / 0.05  (t ∈ C),  그 외 1

w_k  = (1 − p'_k) · Π_{t∈C, t<k} p'_t   첫 오류가 후보 k일 가중
S    = Π_{t∈C} p'_t                     첫 오류가 후보 밖 어딘가일 가중.  Σ w_k + S = 1

r_lo(t) = Σ_{k>t} w_k                   t까지 오류가 없을 확률의 하한
r_hi(t) = r_lo(t) + S                   상한

라벨:  r_lo(t) ≥ 0.9 → 1
       r_hi(t) ≤ 0.1 → 0
       마지막 스텝 → 0
       그 외 → 마스크
```

곱에 들어가는 것은 후보 스텝뿐이라 풀이 길이가 결과에 영향을 주지 않는다. 남는 곱(뒤 후보에 앞 후보의 p'가 곱해지는 것)은 "첫 오류가 6번이려면 3번은 오류가 아니었어야 한다"는 논리 조건이다.

정규화로 나누지 않고 S를 "위치 미상"으로 남겨 두는 이유는, 약한 후보 하나만 있는 풀이에서 그 후보가 확률 1을 받는 것을 막기 위해서다. 후보가 없으면 S=1이 되어 마지막 스텝만 0이고 나머지는 마스크된다. 결과 항만 남는 것과 같다.

π_t/0.05는 보정된 확률이 아니라 단조 변환이다. 최종 라벨의 정밀도는 4.4절의 감사에서 확인한다.

### 2.5 예시

8스텝 오답 풀이에서 후보가 3번(p'=0.2), 6번(p'=0.06), 8번(p'=0.4)일 때 w = (0.80, 0.188, 0.007), S = 0.005다.

```
t       1     2     3     4     5     6     7     8
r_lo   0.995 0.995 0.195 0.195 0.195 0.007 0.007  0
r_hi   1.000 1.000 0.200 0.200 0.200 0.012 0.012  0
라벨    1     1     ·     ·     ·     0     0     0
```

첫 오류가 3번인지 6번인지는 모른 채로, 두 가설이 일치하는 구간만 하드 라벨이 된다. 같은 점수 곡선의 정답 풀이는 [1,1,1,1,1,1,1,1]이다.

## 3. 번역 다리

분절 규칙은 한국어 쪽에서 정한다. 생성 프롬프트에서 빈 줄로 스텝을 구분하게 하고 스텝을 하나씩 번역하므로 스텝 대응은 구성상 1:1이다. 교사 입력은 2.2절대로 스텝 리스트에서 직접 조립하므로 재분절 단언과 DTW는 필요 없다.

번역기는 gemma-4-12B-it(지시 프롬프트) 하나다. 폴백 모델은 두지 않는다. 같은 300문제 파일럿(MATH 150 + GSM8K 150)에서 복원율은 gemma-3-12b-it가 프롬프트·파싱 수정 전 93.7%(281/300), 수정 후 97.0%(291/300)였고, gemma-4-12B-it는 수정 전 99.3%(298/300), 수정 후 99.7%(299/300)였다. 남은 실패 1건은 자리표시자 중복이고, gemma-3-12b-it가 성공한 문제는 모두 gemma-4-12B-it도 성공했다. 원래 후보였던 TranslateGemma-12B는 시스템 프롬프트를 받지 않아 자리표시자 보존을 지시할 수 없고 복원율도 89.0%로 낮아 뺐다. gemma-4-12B-it는 vLLM 0.29 이상이 필요하다(0.15의 레지스트리에 `Gemma4UnifiedForConditionalGeneration`이 없다). 마스킹은 전처리로 한다.

1. 수식 마스킹. `$...$`, `\boxed{...}`, 독립 숫자 토큰을 `⟦M1⟧` 형태의 자리표시자로 바꾼다.
2. 스텝 단위 번역. 문맥 없이 스텝 하나씩 번역한다.
3. 복원과 검사. 자리표시자가 누락되거나 중복되면 그 풀이(문제 번역이면 그 문제)를 버리고, 버린 비율을 보고한다. 순서가 바뀐 것은 어순 차이이므로 정상으로 본다. 번역기를 둘 유지하며 재시도하는 안은 뺐다.

번역이 만든 가짜 하강은 커널이 걸러내지 못한다. 이를 임베딩 모델(LaBSE)로 이중 검사하는 안은 뺐다. 번역기를 못 믿는 상황에서 다른 모델의 유사도 점수를 믿을 근거가 없고, 번역이 라벨에 끼치는 잡음은 4.4절의 감사가 사람 라벨 대비로 직접 잰다. 통제 수단은 자리표시자 복원 검사와 감사 수치 둘뿐이다.

자리표시자 규칙. 스텝 헤더(`## 단계 N:`)의 번호는 마스킹하지 않는다(자리표시자 밀도가 실패의 주원인). 번역 결과에 목표 언어가 아닌 문자(영어 출력의 한글·한자, 한국어 출력의 한자)가 자리표시자 밖에 남으면 실패로 처리한다. 수식만 있는 스텝은 번역기를 거치지 않고 그대로 둔다(번역기가 내용을 지어내기 때문). `$...$`는 내용으로 판정한다. 후보 안에 줄바꿈이 있거나, `\text{}` 밖에 한글이 있거나, 산문 단어가 셋 이상 연속하거나, `$5 ... $8`처럼 숫자로 열고 닫는 `$` 뒤에 바로 숫자가 오면 수식이 아니라 통화 기호로 보고 첫 `$`를 일반 문자로 넘긴다(GSM8K 문제의 18%가 이 경우였다).

파일럿(EXAONE 100풀이, 675스텝)의 스텝 단위 복원율(번역기 선정 당시, gemma-3-12b-it 기준): gemma-3-12b-it 98.7%(풀이 단위 92%), Qwen2.5-7B-Instruct 94.8%(77%). 번역된 영어 스텝의 6~8%에 한글이 남는데 대부분 `\text{인치}`처럼 마스킹된 수식 안이다. 이것은 손대지 않고 한계로 둔다.

A의 영→한 번역과 감사 세트의 왕복에도 같은 파이프라인을 쓴다. 문제 번역(영→한)도 같다. MATH와 GSM8K는 영어 원문이 있으므로 교사에게 주는 문제는 번역하지 않는다.

## 4. 데이터

### 4.1 분할 (가장 먼저)

- dev: MATH train에서 레벨 층화로 500문제를 떼어 둔다. 생성 전에 문제 단위로 뗀다.
- train 풀: MATH train과 GSM8K train에서 dev를 뺀 약 1.45만 문제
- PRM800K: 학습용 표본(A), 감사용 held-out(오답 500, 정답 최대 500)
- test: KO MATH500. 최종 후보에 한 번만 쓴다.

### 4.2 B: 생성과 선택

생성기는 EXAONE-4.0-1.2B와 Qwen2.5-3B, T=0.8, 문제당 생성기별 2풀이다. 약 5.8만 풀이에 y를 붙인 뒤, 비싼 단계(번역과 72B 채점) 앞에서 y만으로 고른다.

풀이 생성기는 작은 모델이 맞다. 우리 PRM이 평가 때 채점하는 것이 1.2~3B 생성기의 한국어 풀이이므로 학습용 풀이도 같은 생성기에서 나와야 한다. 큰 생성기는 대부분의 문제를 맞혀서 정답과 오답이 섞인 문제가 줄고, 오류의 종류도 작은 모델의 것과 달라진다. 큰 생성기의 풀이에서 얻을 그럴듯한 오답은 A가 이미 담당한다. 큰 모델이 필요한 자리는 라벨을 붙이는 쪽이고, 그 자리는 교사 72B가 채우고 있다.

- 정답과 오답이 같이 나온 문제만 남긴다. 검증기가 결과를 바꿀 수 있는 문제는 그런 문제뿐이고, BoN과 DVTS는 같은 문제 안에서만 비교한다.
- 문제당 정답 1개와 오답 1개를 생성기 균형을 맞춰 무작위로 뽑는다.
- 6천 문제, 1.2만 풀이를 상한으로 한다. 평가가 MATH500이므로 MATH의 섞인 문제를 먼저 채우고 나머지를 GSM8K로 채운다.

### 4.3 A: PRM800K 번역

phase 2의 `pre_generated_steps`(전체 풀이)를 영→한으로 옮긴다. 사람 라벨은 B와 같은 형식으로 맞춘다.

- `found_error`: 첫 −1 앞은 1, 그 스텝부터 끝까지 0
- `solution`: 전 스텝 1
- neutral(0)은 1로 둔다. 접두를 죽이지 않기 때문이다.
- 정답과 오답을 가능한 한 1:1로 맞추고, 정답 풀이가 모자라면 있는 만큼 쓴다.

필드 구성은 1일차에 데이터를 열어 확인한다. 풀이가 첫 오류에서 잘려 있다면 감사에서 "마지막 스텝은 0" 규칙이 정답을 그대로 알려 주는 셈이 되므로, 전체 풀이 필드가 있는지가 확인 대상이다.

### 4.4 감사 세트

사람 라벨은 영어 풀이에만 있으므로, PRM800K held-out 풀이를 영→한으로 옮겨 한국어 풀이를 만든 뒤 B와 똑같은 절차(한→영, 교사, 커널)에 태운다. 기준 분포 D는 held-out의 정답 풀이에서 따로 만든다.

- 라벨 정밀도: 마스크되지 않은 스텝 라벨이 사람의 첫 오류 위치와 맞는 비율. 목표 90%
- 커버리지: 마스크되지 않은 스텝의 비율
- 다리의 비용: 같은 풀이의 영어 원문에 교사를 바로 태웠을 때와의 정밀도 차이

번역을 두 번 거친 번역문 위에서 재는 것이라 실제(편도, 원문)보다 불리한 쪽의 추정이다. 생성이 필요 없으므로 2일차에 먼저 돌려 B의 진행 여부를 판단한다.

### 4.5 학습 세트

A, B, A+B 각각을 3천, 6천, 1.2만 풀이로 만든다. 3천 ⊂ 6천 ⊂ 1.2만으로 중첩되게 자르고, A+B는 각 크기에서 반씩 섞는다.

스키마: problem_id, problem_source, problem_ko, problem_en, arm, generator, solution_steps, solution_steps_en, teacher_logodds, outcome, ceiling, candidates, tail_probs, r_lo, r_hi, step_labels, mask_restore_ok.

## 5. 파이프라인 순서

1. 분할 고정 (4.1)
2. 번역 파이프라인 구축, z_t 추출 함수, 학생 백본 점검(6.2절), 100풀이 파일럿
3. 문제 번역(영→한), train 풀과 dev 생성, y 채점
4. 감사: held-out 왕복, 교사 채점, 커널, 정밀도 확인. 3번과 병렬
5. 선택 (4.2)
6. B 라벨링: 스텝 번역, 교사 채점, 기준 분포, 커널
7. A 구성 (4.3). 3~6번과 병렬
8. 학습 세트 조립 (4.5)
9. 학습 11회 (6절)
10. dev에서 체크포인트 선택
11. MATH500 최종 평가와 참조선

## 6. 손실과 학습

### 6.1 손실

```
L = CE(y, p_T) + (1/|M|) · Σ_{t∈M} CE(ℓ_t, p_t)
M = 마스크되지 않은 스텝 (마지막 스텝 T 제외).  M이 비면 첫 항만
```

p_t는 스텝 끝 구분 토큰 위치의 2-class 로짓에 softmax를 건 양성 확률이고, 하드 라벨이므로 2-class CE와 BCE는 같다. 스텝 항을 풀이 안에서 평균하므로 긴 풀이나 긴 0 꼬리가 손실을 지배하지 않는다.

### 6.2 학생 구조와 폴백

학생은 Qwen2.5-Math-PRM과 같은 구조로 만든다. 공개된 `modeling_qwen2_rm.py`의 `Qwen2ForProcessRewardModel`로 확인한 구조는 다음과 같다.

- 백본은 수정 없는 `Qwen2Model`이다. 어텐션, 마스크, 위치 인코딩 어디에도 변경이 없다.
- LM 헤드 자리에 `Linear(h, h) → ReLU → Linear(h, 2)` 헤드가 얹혀 있고, 손실은 토큰 위치의 2-class CE다.
- `<extra_0>`는 어휘에 있는 토큰 하나이며, 모델은 모든 위치에 로짓을 내고 호출하는 쪽이 그 토큰 위치만 읽는다.

즉 구조 변경이 아니라 토큰 하나에 역할을 주고 헤드를 바꾼 것이다. 따라서 어떤 인과 LM 백본에도 같은 방식으로 헤드를 얹을 수 있다.

- 기본: EXAONE-4.0-1.2B 백본에 위 헤드를 얹는다. 스텝 끝 구분 토큰은 어휘의 미사용 특수 토큰을 쓰고, 없으면 하나 추가해 임베딩을 늘린다. 영어 PRM 체크포인트에서 출발하지 않는다(12.2절 참고).
- 1일차 점검: 구분 토큰이 단일 id로 인코딩되는지, 1배치 순전파와 역전파가 도는지, 구분 토큰 위치의 로짓이 2.2절의 함수로 그대로 추출되는지.
- 폴백: 점검에서 막히면 백본을 Qwen2.5-1.5B-Instruct로 바꾼다. 교사와 같은 Qwen2 구조라 `Qwen2ForProcessRewardModel`과 `<extra_0>`를 그대로 쓸 수 있고, 하네스 수정도 없다. 이 경우 홀드아웃 생성기와 학생 백본이 같은 모델이 되는데, 홀드아웃은 그 생성기의 출력이 학습 데이터에 없다는 뜻이므로 증거의 성격은 유지된다.

학습률 1e-5, 워밍업 3%, 코사인, 배치 64풀이, 최대 길이 2048, bf16, 3에폭. 전 실행에 같은 값을 쓴다.

- 본 실행 9회: {A, B, A+B} × {3천, 6천, 1.2만}
- 절제 2회(B, 1.2만): 결과 항만, 교사 원값 소프트 타깃(ℓ_t 대신 σ(z_t))

## 7. 평가

- 집계: 학생의 라벨이 누적 의미이므로 풀이 점수는 마지막 스텝 점수(last)를 기본으로 하고, min을 보조로 같이 낸다. Math-Shepherd는 같은 라벨 정의에서 min을 썼다. 교사 기준선은 스텝별 의미이므로 min을 쓴다. 곱은 긴 풀이에 불리하므로 어디에도 쓰지 않는다.
- 선택: dev 500문제 × 생성기 2 × 16샘플을 만들어 두고 점수만 다시 매겨 BoN n=16의 NAIVE와 WEIGHTED로 고른다.
- 최종: KO MATH500, BoN n=1~64, NAIVE/WEIGHTED/MAJ. 생성기는 EXAONE-4.0-1.2B, Qwen2.5-3B, 홀드아웃 Qwen2.5-1.5B(저장된 64샘플 사용). DVTS는 최종 후보 하나에만 돌린다.
- 주 평가 생성기: 학생과 크기가 같은 EXAONE-4.0-1.2B로 고정하고, 3B의 결과는 보조 지표로 읽는다. Math-Shepherd는 작은 보상 모델로 큰 생성기를 검증하면 다수결보다 나빠지고, 7B 보상 모델은 후보 수가 늘수록 정확도가 떨어지는 경향을 보였다고 보고했다. 같은 이유로 n=16과 n=64를 따로 보고한다. n이 커질 때 학생의 BoN이 꺾이는지가 작은 PRM의 한계가 가장 먼저 드러나는 지점이다.
- 참조선: 현행(영어 PRM을 한국어 원문에 직접, min), 번역 경유 교사(생성기 1개, n=16, min)
- 통계: 문제 단위 대응 부트스트랩 구간

보고 항목: 자리표시자 복원 성공률, 한·영 스텝 수와 길이 분포, 생성기별 천장 분포와 하강폭 분포, 커널 라벨 구성비(1/0/마스크), 후보 없는 오답 풀이 비율, 감사 세트의 정밀도·커버리지·다리의 비용, 학생의 첫 오류 식별 정확도(감사 세트, 사람 라벨 기준), n=16과 n=64의 BoN 추이, 학습 곡선, 절제 결과.

## 8. 계산량 (대략)

| 항목 | 총량 | 비고 |
|---|---|---|
| 문제 번역(영→한) | 1.5만 문제 | MATH·GSM8K train |
| 생성(1.2~3B) | train 풀 5.8만 + dev 1.6만 풀이, 약 3,000만 토큰 | v8.1 추정 기준 A100 9시간 |
| B 스텝 번역(한→영) | 1.2만 풀이, 약 2,400만 토큰 | 선택 후에만 |
| A 번역(영→한) | 1.2만 + 감사 1천 풀이 | |
| 감사 왕복(한→영) | 1천 풀이 | |
| 교사 채점(72B 순전파) | B 1.2만 + 감사 2천 = 1.4만 회 | 캐시 |
| 참조선(번역 경유 교사) | 500 × 16 = 8천 풀이 번역·채점 | 생성기 1개 |
| 학습(1.2B) | 11회, 회당 최대 1.2만 풀이 × 3에폭 | |

v8.1 대비 교사 채점은 5분의 1, 번역은 A와 참조선을 합쳐도 절반 수준이고 MC는 없다.

## 9. 결정 분기

| 질문 | 기본값 | 답이 다르면 |
|---|---|---|
| 학생 백본 점검(6.2절) | EXAONE-4.0-1.2B 통과 | 막히면 Qwen2.5-1.5B-Instruct에 `Qwen2ForProcessRewardModel`을 그대로 쓴다. 다른 백본을 새로 시도하지 않는다 |
| 자리표시자 복원 실패율 | 15% 미만 가정 | 넘으면 마스킹 단위를 수식 전체에서 숫자만으로 좁힌다 |
| 감사 정밀도 | 0.9/0.1 임계에서 90% 이상 | 미달이면 0.95/0.05로 한 단계 조인다. 그래도 85% 미만이면 B의 스텝 라벨을 쓰지 않고 A와 결과 항 구성으로 가며, 다리의 비용을 결과로 보고한다 |
| 섞인 문제 수 | 6천 이상 | 모자라면 곡선의 최대 점을 가용량으로 낮춘다 |
| GPU | 4장 | 2장이면 교사 채점을 8비트 양자화로 돌린다. 현재 환경은 공유 서버의 A6000 48GB 8장이므로 교사는 fp8 없이 bf16 TP=4로 돌린다. 2장만 쓸 수 있는 상황으로 내려가면 vLLM 풀링 러너에 fp8 가중치 양자화(Ampere용 W8A16 Marlin), TP=2로 돌린다 |

## 10. 일정 (GPU 4장)

| 일차 | 내용 |
|---|---|
| 1 | 분할 고정, PRM800K 필드 확인, 번역 파이프라인과 z_t 추출, 학생 백본 점검(막히면 즉시 폴백), 100풀이 파일럿, 문제 번역 시작 |
| 2 | train 풀과 dev 생성, y 채점. 감사 세트 왕복과 교사 채점, 커널 정밀도 확인 |
| 3 | 선택, B 스텝 번역·교사 채점·커널 라벨. A 번역 |
| 4 | 학습 세트 조립, 본 실행 9회와 절제 2회, dev 선택 |
| 5 | MATH500 최종 평가, 참조선, 최종 후보 DVTS |
| 6 | 부트스트랩, 보고 항목, 문서화 |
| 예비 | 실패 복구. 남으면 12절에서 하나 |

## 11. 알려진 한계

- y=1 풀이를 전부 1로 두므로, 과정은 틀렸는데 답만 맞은 풀이의 스텝도 1이 된다. 교사 논문은 MATH에서 정답 응답의 11.9%가 그런 경우라고 보고했다.
- 번역이 만든 가짜 하강은 교사의 확신처럼 보여서 커널이 구분하지 못한다. 감사 수치로만 통제한다.
- 감사는 번역문 위의 왕복 측정이라 한국어 원문 쪽 라벨 잡음은 간접 추정이다.
- 첫 오류 후보가 갈리는 풀이는 가운데 구간이 마스크되어, 가장 정보가 많은 스텝을 버리게 된다. 12.1절이 그 구간을 채우는 방법이다.
- 학생(1.2B)이 Qwen2.5-3B 생성기보다 작아서 3B 쪽 결과는 불리한 조건에서 나온다. 12.2절이 이에 대한 대응이기도 하다.
- 홀드아웃 생성기가 학습 생성기와 같은 계열이라 생성기 일반화 증거로는 약하다.

## 12. 가능성 (메인 계획 밖)

### 12.1 한 점 MC

커널이 비워 둔 구간을 채우는 선택지다. 오답 풀이에서 마스크 구간의 마지막 스텝까지를 그 풀이 자신의 접두로 주고 완성 모델이 8번 이어 쓰게 한 뒤, 최종 답만 본다. 접두는 원래 풀이의 앞부분을 글자 그대로 자른 것이고, 이어 쓴 풀이는 분절하지도 비교하지도 않으므로 라벨은 원래 풀이의 스텝에만 붙는다.

- 하나라도 정답에 닿으면 그 접두는 1이므로 마스크 구간을 1로 채운다.
- 전부 실패는 약한 증거다. 성공률 10%인 멀쩡한 접두도 43% 확률로 0/8이 나온다. 그대로 마스크로 둔다.
- 완성 모델은 가능하면 32B급 이상을 쓴다. Math-Shepherd는 완성기가 클수록 주석 품질이 좋아진다고 보고했고, 풀이당 한 지점 8회라 감당할 수 있다.
- 비용은 마스크 구간이 있는 풀이당 8회로, 스텝마다 8회씩 도는 방식의 스텝 수 분의 1이다.
- 넣는다면 "B + 한 점 MC"를 1.2만 크기 하나에서 넷째 팔로 돌린다. B와의 차이가 MC로 끊어 주는 것의 기여다.
- MC 없이 되는 변형이 하나 있다. 같은 문제의 정답 풀이와 앞부분 텍스트가 m번째 스텝까지 똑같으면 m번까지는 1로 채울 수 있다. 채워지는 양은 적을 것이다.
- 보고용으로만 쓴다면 B의 확신 라벨 300풀이에 같은 절차를 돌려 원문 쪽 잡음률을 직접 잰다.

### 12.2 영어 PRM 체크포인트 초기화

절대 성능까지 보여야 한다면 같은 B 라벨을 Qwen2.5-Math-PRM-7B에서 출발하는 미세조정에 쓴다. 이 경우 1.2만 풀이는 검증 능력 전체가 아니라 언어 격차만 메우면 된다. 학생이 모든 생성기보다 커지므로 크기 관계의 문제도 같이 풀린다. DVTS용으로는 7B가 무겁다는 점이 대가다.

### 12.3 제3 계열 홀드아웃

생성기 일반화 증거를 강하게 하려면 Gemma-3-4B-it 같은 제3 계열 생성기를 평가 전용으로 추가한다. 생성 비용만 든다.

### 12.4 최신 Qwen 계열로 교체

메인 계획이 끝나고 시간이 남을 때만 한다. 교체하면 현행 수치와 저장된 MATH500 64샘플이 전부 옛 생성기의 것이어서 연속성이 끊기므로, 새 생성기로 64샘플을 다시 만들고 현행 기준선도 다시 재야 한다. 생성과 채점에 반나절 정도 든다. 메인 실행과 섞지 않고 별도 실험으로 둔다.

| 역할 | 메인 | 교체안 |
|---|---|---|
| 학습 생성기 | EXAONE-4.0-1.2B, Qwen2.5-3B | Qwen3.5-0.8B, Qwen3.5-2B (비사고 모드) |
| 학생 백본 | EXAONE-4.0-1.2B | Qwen3.5-4B-Base |
| 홀드아웃 생성기 | Qwen2.5-1.5B | EXAONE-4.0-1.2B (저장된 64샘플 그대로) |
| 12.1절 완성기 | Qwen2.5-7B-Instruct | Qwen3.6-35B-A3B |
| 교사 | Qwen2.5-Math-PRM-72B | 그대로 |

모델 카드에서 확인한 것: Qwen3.5-2B는 Apache 2.0, 201개 언어, 비사고 모드가 기본이고 사고 모드는 `enable_thinking`으로 켠다. 2B는 사고 모드에서 루프에 빠지기 쉽다고 적혀 있다. Gated DeltaNet과 어텐션을 3:1로 섞은 하이브리드 구조에 비전 인코더가 붙어 있고, 카드 작성 시점 기준 vLLM과 transformers 모두 main 브랜치가 필요했다. vLLM에는 `--language-model-only` 옵션이 있다. 카드에 MATH500, GSM8K 수치는 없다.

- 생성기 크기 규칙: dev에서 한국어 평균 정답률이 35~75%에 드는 크기만 학습 생성기로 쓴다. 최신 생성기는 MATH를 훨씬 잘 풀어서 섞인 문제가 줄고, 그러면 BoN으로 PRM이 끌어올릴 몫도 준다. 4B를 생성기에서 뺀 이유다. GSM8K는 2B에게 거의 다 풀릴 가능성이 높아서 섞인 문제는 대부분 MATH에서 나올 것이고, 모자라면 강한 생성기 쪽만 문제당 풀이 수를 4로 올린다.
- 사고 모드는 끈다. 길고 되돌아가는 사고 흔적에서는 첫 오류 라벨의 정의가 성립하지 않는다.
- 샘플링: 카드는 비사고 텍스트 작업에 temperature 1.0, presence_penalty 2.0을 권장하지만 수학 풀이에는 페널티가 과하다. T=0.8에 페널티 없이 돌리고, 길이 상한을 두어 끝나지 않은 풀이는 버린다.
- 학생 구현: 하이브리드 구조에 비전 래퍼가 붙어 있어 6.2절의 헤드를 얹는 기성 클래스가 없을 수 있다. 언어 모델 부분만 로드해 헤드를 직접 붙이고, 6.2절과 같은 1배치 점검을 거친다. 막히면 표준 구조인 Qwen3-4B-Base로 내려간다.
- 이 구성에서는 학생(4B)이 두 생성기보다 커져서 크기 관계의 불리함이 사라지고, EXAONE이 홀드아웃이 되어 다른 계열 홀드아웃이 된다.

## 13. 변경 이력

### 13.0 v9.2에서 v9.3으로 (2026-09-20)

- 번역기를 gemma-3-12b-it에서 gemma-4-12B-it로 바꿨다. 같은 300문제 파일럿(MATH 150 + GSM8K 150, 같은 코드)에서 복원율은 gemma-3-12b-it가 프롬프트·파싱 수정 전 93.7%(281/300), 수정 후 97.0%(291/300)였고, gemma-4-12B-it는 수정 전 99.3%(298/300), 수정 후 99.7%(299/300)였다. 남은 실패 1건은 자리표시자 중복이다. gemma-3-12b-it가 성공한 문제는 모두 gemma-4-12B-it도 성공했다.
- 실행 환경을 vLLM 0.29.0 + transformers 5.17 + torch 2.13의 단일 venv로 올렸다. gemma-4-12B-it가 vLLM 0.15의 레지스트리에 없고(`Gemma4UnifiedForConditionalGeneration`), vLLM 0.29는 transformers 5.10.4 이상을 요구한다. 검증: 교사 7B의 로그 오즈를 0.15에서 저장한 값과 대조해 16/16 풀이의 스텝 수가 일치하고 최대 |Δz| = 0.077, 학생 점검 통과(EXAONE-4.0-1.2B, `[unused0]` id 62), CPU 학습 스모크 통과, EXAONE 생성과 y 채점 통과, 테스트 106개 통과. 교사 채점은 `LLM.reward` 대신 `LLM.encode(..., pooling_task="token_classify")`를 쓴다. 이 서버에서는 flashinfer 샘플러의 JIT 빌드가 ninja·nvcc를 요구해 실패하므로 `VLLM_USE_FLASHINFER_SAMPLER=0`으로 돌린다(`koprm/paths.py`에서 설정한다).
- 번역 실패의 원인을 프롬프트와 파싱에서 고쳤다. 자리표시자 규칙과 스텝 헤더 규칙을 단위마다 조건부로 넣어(그 단위에 없으면 규칙도 빼) 모델이 "## 단계 2" 같은 이어쓰기와 꼬리 자리표시자를 지어내는 것을 막고, 그래도 나오면 출력에서 잘라낸다. `[asy] ... [/asy]` 블록은 자리표시자 하나로 마스킹한다. 실패한 행은 모델 원문(`raw_failed`)과 함께 기록해 원인을 파싱·로직에서 고칠 수 있게 했다.
- 서버 환경을 적었다. A6000 48GB 8장, HF 캐시는 공유 경로 `/srv/lilab/hf_cache`다.

### 13.1 v9.1에서 v9.2로 (구현 착수, 2026-09-20)

- LaBSE 내용 이동 검사(옛 3.4절)를 뺐다. 번역기를 못 믿으면 다른 모델의 유사도도 믿을 수 없고, 번역 잡음은 감사 세트가 사람 라벨 대비로 직접 잰다. 스키마의 labse_flags와 보고 항목의 플래그 비율도 지웠다.
- 실행 환경을 적었다. 공유 서버(A6000 48GB × 8) 중 2장만 쓸 수 있으므로 교사 채점은 vLLM 풀링 러너 + fp8 가중치 양자화 + TP=2다. 슬럼은 없고 GPU 단위로 프로세스를 나눠 돌린다.
- 교사 점수 추출(2.2절)은 vLLM 풀링 러너에서 활성화 함수를 끄고 헤드를 float32로 돌려 `<extra_0>` 위치의 2-class 로짓을 직접 받는다. 1일차 점검으로 HF 참조 구현의 확률과 대조한다.
- TranslateGemma가 게이트 모델이라 접근 토큰이 준비될 때까지 캐시된 지시 모델을 임시 번역기로 쓴다(3절).
- 분할을 확정했다. dev 500(레벨 1~5: 38/90/106/113/153), train 풀 14,473(MATH 7,000 + GSM8K 7,473). MATH500과 겹치는 문제는 없다.
- PRM800K phase 2를 열어 확인했다. `pre_generated_steps`에 전체 풀이가 있고, 오류 스텝에서는 후보 완성 여러 개가 함께 평가되어 `chosen_completion`이 비어 있으므로 사전 생성 스텝과 텍스트를 맞춰 평점을 읽는다. 쓸 수 있는 풀이 73,949개(found_error 64,181, solution 9,768), 문제 10,801개. 첫 오류 위치 평균 5.1(0부터), 마지막 스텝이 첫 오류인 비율 3.0%. MATH500과 겹치는 문제 0개.
- 파일럿(EXAONE-4.0-1.2B, KO MATH500 50문제 × 2, 배관 점검용): 풀이당 평균 6.8스텝, 473토큰, 길이 초과 없음, 정답률 56%. math_verify 채점의 오탐은 44개 오답을 전수 확인해 0건. 생성기가 프롬프트의 "[간결한 설명]"을 그대로 베끼는 버릇이 있으나 라벨링에는 무관하다.
- 번역기(3절): TranslateGemma 대신 gemma-3-12b-it를 주 번역기로 정했다. 수치는 3절에 있다.
- 학생 백본 점검(6.2절) 통과. EXAONE-4.0-1.2B의 `[unused0]`(id 62)이 단일 토큰 구분자이고 임베딩 확장이 필요 없다. 폴백은 쓰지 않는다. 폴백용으로 확인한 Qwen2.5-1.5B-Instruct는 `<extra_0>`를 특수 토큰으로 추가하면 된다.
- 평가(7절)는 komath 하네스를 다시 돌리지 않고, 저장된 KO MATH500 64샘플(생성기 3종)을 학생으로 다시 채점한다. 재구현한 집계가 komath의 저장 결과(EXAONE, 현행 PRM: naive@1 .552, naive@64 .686, maj@64 .712, weighted@64 .720)를 1.4%p 이내로 재현했다.
- 교사 점수 추출 점검: 7B로 vLLM 풀링 로짓과 HF 참조를 대조해 스텝 수가 전부 일치하고 |Δz| ≤ 0.2(fp32 참조 기준). 남는 차이는 bf16 잡음이다.
- 감사 세트(4.4절): 오답 500 + 정답 500, 684문제. A 풀 65,964풀이(9,668문제, y=1 12.3%)와 문제가 겹치지 않고 dev·MATH500과도 겹치지 않는다.
- 코드는 `koprm/` 패키지에 단계별 모듈로 두고, 생성·번역·채점은 모두 재개 가능한 jsonl 샤드로 쓴다. y 채점은 komath의 math_verify 사용법을 따르되 마지막 `\boxed{}`를 중괄호 균형으로 뽑고 프로세스 풀에 항목별 타임아웃을 건다.

### 13.2 v9에서 v9.1로

- 학생 구조(6.2절): Qwen2.5-Math-PRM의 공개 코드로 구조를 확인해 고정했다. 백본은 수정 없는 Qwen2Model, 헤드는 Linear-ReLU-Linear(2), 손실은 토큰 위치의 2-class CE이고 `<extra_0>`는 위치를 읽는 토큰일 뿐이다. 학생은 EXAONE-4.0-1.2B에 같은 헤드를 얹고, 1일차 점검에서 막히면 Qwen2.5-1.5B-Instruct에 그 클래스를 그대로 쓴다. 결정 분기와 일정에 반영했다.
- Math-Shepherd 기준의 위치 정리: 2.1절에 라벨 정의가 Math-Shepherd의 HE와 같다는 점과 0의 출처가 다르다는 점을 적었다. 1절에 1만 규모 상대 비교의 선례와 A 대 B의 선례를 붙였다. 참고 문헌에 추가했다.
- PRM과 생성기의 크기 관계(7절, 11절): 주 평가 생성기를 학생과 크기가 같은 EXAONE-4.0-1.2B로 고정하고 3B는 보조로 읽는다. n=16과 n=64를 따로 보고한다. 12.2절이 이에 대한 대응임을 적었다.
- 풀이 생성기는 작은 온폴리시 모델이 맞다는 근거를 4.2절에 적었다. 큰 모델이 필요한 자리는 라벨링이며 교사 72B가 이미 그 자리다.
- 12.1절의 완성 모델을 7B급에서 32B급 이상으로 올렸다.
- 최신 Qwen 계열(Qwen3.5, Qwen3.6)로의 교체를 12.4절로 두었다. 기존 측정과의 연속성이 끊기고 학생 구현 위험이 새로 생기므로 메인에서는 EXAONE과 Qwen2.5를 유지한다.
- 4.3절에 PRM800K 필드 확인의 이유를 적었다. 7절 집계에 min을 보조로 추가했다.

### 13.3 v8.1에서 v9로

- 타깃: MC로 보정한 소프트 곡선에서 y 조건부 하드 라벨로. 교사 논문이 비판한 소프트 라벨은 MC 성공률이고, v8.1의 보정이 교사 확률을 그 눈금으로 끌어내리고 있었다. 방법의 이름도 증류에서 라벨 투영으로 바꿨다. 교사 원값 소프트는 절제로 남겼다.
- MC: 전역·풀이별 보정, MC 손실항, 탐색점이 전부 빠졌다. y가 1회짜리 MC 역할을 하고, 라벨 정밀도 측정은 PRM800K 감사가 맡는다.
- 임계: 교사 확률의 절대 임계(0.5, 0.9)를 버리고, 풀이 자신의 천장 대비 하강폭을 정답 풀이의 분포로 재는 방식으로 바꿨다.
- 길이 편향: 전 스텝의 곱을 후보 스텝만의 곱으로 바꾸고, 손실은 풀이 안 평균, 추론 집계는 last(학생)와 min(교사)으로 정했다.
- 교사 입력: `\n\n` 재조립과 분절 단언, DTW를 지우고 `<extra_0>` 직접 조립으로 바꿨다. 점수는 float32 로그 오즈로 캐시한다.
- 데이터: K2(KSM)를 뺐다. MATH·GSM8K train에서 문제 다양성을 우선해 문제당 2풀이만 쓴다. 문제당 정답 1, 오답 1의 선택을 번역 전에 한다.
- A 팔과 감사 세트를 추가했다. v8.1에서 뺀 PRM800K 번역을 보조 데이터가 아니라 비교 팔과 검사 도구로 되살렸다.
- 평가: 체크포인트 선택을 train에서 떼어 둔 dev로 옮기고 MATH500은 한 번만 쓴다. 성공 기준을 절대 임계 5개에서 상대 비교 3개로 바꿨다.
- 7B 학생과 절제 8종을 1.2B 하나와 절제 2종으로 줄였다.

## 14. 참고

- Zhang et al., The Lessons of Developing Process Reward Models in Mathematical Reasoning. arXiv:2501.07301
- Qwen2.5-Math-PRM-72B 모델 카드. https://hf.co/Qwen/Qwen2.5-Math-PRM-72B
- Qwen2.5-Math-PRM-7B의 `modeling_qwen2_rm.py`(`Qwen2ForProcessRewardModel`). https://hf.co/Qwen/Qwen2.5-Math-PRM-7B
- Wang et al., Math-Shepherd: Verify and Reinforce LLMs Step-by-step without Human Annotations. arXiv:2312.08935
- Lightman et al., Let's Verify Step by Step. arXiv:2305.20050
- Wang et al., Demystifying Multilingual Reasoning in Process Reward Modeling. arXiv:2502.12663
- Duan et al., Efficient Process Reward Model Training via Active Learning. arXiv:2504.10559
- Sohn et al., FixMatch. arXiv:2001.07685
- Qwen3.5-2B 모델 카드. https://hf.co/Qwen/Qwen3.5-2B
